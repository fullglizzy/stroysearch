-- Потолок счёта больше не заменяет строки платежа: излишек над потолком
-- теперь становится общей скидкой по счёту. Строка шаблона "cap" не
-- используется — удаляем (только данные, схема не меняется)

DELETE FROM "doc_template_lines" WHERE "docKind" = 'billing_invoice' AND "code" = 'cap';

# План улучшений: QOL, дизайн, UX — ЕНЦПР

## Объём: 33 улучшения, сгруппированы в 6 фаз

---

## Фаза 1: Foundation (инфраструктура сайта)

### 1.1. Toast-уведомления (#19)
- **Монтировать `<Toaster>` в `src/app/layout.tsx`** — компонент уже готов в `src/components/ui/toast.tsx`
- **Создать `src/lib/toast.ts`** — хелпер-обёртки `toastSuccess(msg)`, `toastError(msg)` для единообразного вызова
- **Заменить все `Alert`-success на toast**: `ProfileForm`, `CompanyProfileForm`, `ReviewForm`, `ContentManager`, `FinancesManager`, `CompanyProductsManager`
- **Заменить все `Alert variant="destructive"` на toast-error** где уместно (не во всех случаях — ошибки валидации в форме лучше оставлять рядом с полями)

### 1.2. Тёмная тема + автоопределение + переключатель (#14)
- Установить `next-themes` (лёгкая библиотека, 2KB)
- Создать `src/providers/ThemeProvider.tsx` — обёртка `<ThemeProvider>` с `attribute="class"`, `defaultTheme="system"`, `enableSystem`
- Создать `src/components/shared/ThemeToggle.tsx` — кнопка с иконками `Sun`/`Moon`, использует `useTheme()`
- Добавить `ThemeProvider` в `src/app/layout.tsx`
- Добавить `ThemeToggle` в `Header.tsx` (рядом с кнопкой мобильного меню)

### 1.3. Кастомные страницы ошибок (#37, #38)
- Создать `src/app/not-found.tsx` — стилизованная 404 с ссылками на главную и ключевые разделы
- Создать `src/app/error.tsx` — `"use client"` error boundary с кнопкой «Попробовать снова» и ссылкой в поддержку

### 1.4. Offline-детектор (#42)
- Создать `src/components/shared/OfflineBanner.tsx` — фиксированный баннер внизу экрана «Нет соединения с интернетом», появляется при `navigator.onLine === false`
- Подключить в `layout.tsx`

### 1.5. Кнопка «Наверх» (#11)
- Создать `src/components/shared/ScrollToTop.tsx` — круглая кнопка, появляется при скролле >300px, плавный `scrollTo({top: 0, behavior: "smooth"})`
- Подключить в `layout.tsx`

### 1.6. Скелетоны (skeleton loaders) (#9)
- Создать `src/components/shared/TableSkeleton.tsx` — скелетон таблицы (5-10 строк)
- Создать `src/components/shared/CardSkeleton.tsx` — скелетон карточки/грида
- Обернуть все публичные страницы в `<Suspense fallback={<TableSkeleton />}>`:
  - `suppliers/page.tsx`
  - `library/page.tsx`
  - `conferences/page.tsx`
  - `polls/page.tsx`
  - `matrix/page.tsx`

### 1.7. Хлебные крошки (#13)
- Создать `src/components/shared/Breadcrumbs.tsx` — принимает массив `{label, href?}[]`, рендерит с разделителем `/`
- Добавить на все страницы дашборда и публичные страницы

### 1.8. Анимации переходов (#10)
- Создать `src/components/shared/PageTransition.tsx` — обёртка с `animate-in fade-in slide-in-from-bottom-2 duration-300`
- Обернуть `children` в `layout.tsx` (main) в этот компонент

### 1.9. Футер (#12)
- Добавить в `Footer.tsx`: секцию с контактами (email поддержки), иконки/ссылки на соцсети-заглушки, более выразительный разделитель
- Добавить логотип ЕНЦПР (текстовый, пока нет загружаемого)

---

## Фаза 2: Формы и валидация

### 2.1. Клиентская валидация всех форм (#30)
Все формы перевести на `react-hook-form` + `zod` (уже установлены):
- **ReviewForm** — валидация comment (≥100 символов), все 9 критериев обязательны
- **ProfileForm** — phone regex, KPP (9 цифр), classifierIds
- **CompanyProfileForm** — то же + INN
- **ContentManager** — pageKey обязателен, content обязателен
- **PollsManager** — вопрос обязателен, мин. 2 варианта ответа
- **CompanyProductsManager** — name, classes (мин 1), price (≥0)
- **TreeConstructor (AddNodeDialog, EditNodeDialog)** — name обязателен

### 2.2. Валидация ИНН (#33)
- Создать `src/lib/inn.ts` с функцией `validateInn(inn: string): boolean` — проверка контрольной суммы для 10-значных (юрлица) и 12-значных (ИП)
- Добавить валидацию в формы: `AddCompanyDialog` (suppliers), `CompanyProfileForm`, `registerCompany`
- Отображать ошибку «Неверный ИНН» при несовпадении контрольной суммы

### 2.3. Статус загрузки в форме поддержки (#40)
- В `HomePageClient.tsx` support-форме уже есть `supportLoading` и `Loader2`. Убедиться что всё корректно. Добавить disabled на все поля.

---

## Фаза 3: Таблицы, пагинация, экспорт

### 3.1. Пагинация (#25)
Создать общий подход:
- **Сервер**: добавить `skip`/`take` параметры в API routes:
  - `api/suppliers/route.ts` (GET)
  - `api/library/route.ts` (GET)
  - `api/conferences/route.ts` (GET)
  - `api/products/route.ts` (GET)
  - `api/polls/route.ts` (GET — добавить handler)
  - `api/admin/tree/route.ts` (GET)
  - Все admin-страницы с таблицами
- **Клиент**: создать `src/components/shared/Pagination.tsx` — кнопки «Предыдущая», номера страниц, «Следующая»
- Добавить пагинацию во все табличные компоненты: `SuppliersPageClient`, `ConferencesPageClient`, `LibraryPageClient`, `PollsPageClient`, `UsersManager`, `ConferencesModeration`, `LibraryModeration`, `CompanyProductsManager`, `PollsManager`

### 3.2. Фильтр по регионам на странице поставщиков (#22)
- Добавить выпадающий список регионов (из `src/lib/regions.ts` — создать справочник регионов РФ)
- Добавить фильтр в `SuppliersPageClient`
- Передавать параметр `region` в API и фильтровать на сервере

### 3.3. Сортировка по рейтингу на странице поставщиков (#23)
- Добавить `orderBy` параметр в `api/suppliers/route.ts`
- Добавить кнопки сортировки в `SuppliersPageClient` (по рейтингу ↑↓, по алфавиту)

### 3.4. Экспорт таблиц в CSV (#44)
- Создать `src/lib/export.ts` с функцией `exportToCSV(rows, columns, filename)`
- Добавить кнопку «Экспорт» на страницы: поставщики, матрица, библиотека, конференции (админка)
- Для админа: экспорт пользователей, счетов

### 3.5. Адаптивные таблицы для мобильных (#15)
- Создать хук `useMediaQuery(query)` или использовать CSS-only подход
- Для `SuppliersPageClient`, `UsersManager`: на экранах <768px показывать карточки вместо таблицы
- Использовать CSS: `hidden md:table-cell` на некритичных колонках, карточный вид через `md:hidden`

---

## Фаза 4: UX-улучшения

### 4.1. Улучшение EyeButton (#16)
- Увеличить область клика до 40×40px (`p-2` вместо `p-1`)
- Добавить `Tooltip` при hover: «Нажмите, чтобы показать телефон»
- Добавить микро-анимацию появления/скрытия данных (через CSS-анимацию строки таблицы)

### 4.2. Индикатор загрузки при клике на глазок (#31)
- В `EyeButton` добавить состояние `loading` — показывать мини-спиннер на время запроса
- Сделать `fetch` внутри `EyeButton` с pending-состоянием (или пробрасывать `onClick` как async)

### 4.3. Refresh после отправки отзыва (#21)
- В `ReviewForm` после успешного ответа API вызывать `router.refresh()` и закрывать диалог
- Добавить toast «Отзыв опубликован! +1 монета»

### 4.4. Модальное подтверждение покупки документа (#29)
- В `LibraryPageClient` при нажатии «Приобрести» показывать `ConfirmDialog`:
  - «Вы уверены, что хотите приобрести документ «{title}» за {price} монет?»
  - После подтверждения — запрос, списание, toast

### 4.5. «Дать аналог» через GuestGuard (#32)
- В `MatrixPageClient` обернуть кнопку «Дать аналог» в `GuestGuard` с `actionLabel="Добавить свой продукт"`
- Проверить ВСЕ места, где кнопка ведёт на `/login` без модалки — заменить на `GuestGuard`:
  - `ProductsPageClient` — кнопки «Добавить свой продукт», «Добавить документ»
  - `ConferencesPageClient` — кнопка «Создать конференцию»
  - `LibraryPageClient` — кнопка «Загрузить документ»
  - `PollsPageClient` — кнопка «Хочу разместить опрос»

### 4.6. Приветственный экран после регистрации (#34)
- После успешной регистрации в `register/page.tsx` и `register/company/page.tsx` не делать сразу `router.push("/account")`
- Вместо этого показать welcome-dialog: «Добро пожаловать на платформу!» с кратким описанием возможностей
- Кнопка «Перейти в личный кабинет»

### 4.7. Массовые действия в админке (#35)
- В `UsersManager`: добавить чекбоксы в строках, кнопки «Зачислить монеты выбранным», «Заблокировать», «Разблокировать»
- В `ConferencesModeration`: «Одобрить выбранные», «Отклонить выбранные»
- В `LibraryModeration`: «Одобрить выбранные», «Отклонить выбранные»

### 4.8. DnD в конструкторе дерева (#43)
- Установить `@dnd-kit/core` + `@dnd-kit/sortable` (лёгкие, 5-8KB gzip)
- Добавить drag-and-drop для перестановки узлов внутри одного родителя
- При drop: PATCH `/api/admin/tree/{id}` с `{action: "move", newParentId, newPosition}`

---

## Фаза 5: Рейтинг 0-100 (#3.4)

### 5.1. Консистентность рейтинга
- Создать `src/lib/rating.ts` с утилитами:
  - `computeRatingFromReviews(reviews): number | null` — среднее weightedAverage × 20
  - `formatRating(rating: number): string` — `"4.3"`
  - `ratingToStars(rating: number): number` — 0-100 → 0-5 звёзд
- Использовать во всех серверных страницах: `suppliers/page.tsx`, `matrix/page.tsx`, админке
- Убедиться, что `StarRating` получает 0-100 и корректно отображает

---

## Фаза 6: Мелкие доделки

### 6.1. Кастомный favicon (#19 в списке пользователя, но это #36 в моём анализе)
- Сгенерировать простой favicon с текстом «ЕЦ» на ментоловом фоне (или использовать эмодзи-фавиконку)
- Заменить `favicon.ico` в `src/app/`

### 6.2. Валидация форм (#30 — см. Фазу 2)

---

## Порядок выполнения

Фазы идут последовательно, внутри фазы — параллельно где возможно:

1. **Фаза 1** (Foundation) — ~10 файлов, не зависит от других
2. **Фаза 2** (Формы) — не зависит от Фазы 3, можно параллельно
3. **Фаза 3** (Таблицы) — использует toast из Фазы 1
4. **Фаза 4** (UX) — зависит от Фазы 1 (toast), можно параллельно с Фазой 3
5. **Фаза 5** (Рейтинг) — автономно
6. **Фаза 6** (Доделки) — после всего

## Файловая структура новых компонентов

```
src/
  components/
    shared/
      Breadcrumbs.tsx        (новый)
      OfflineBanner.tsx      (новый)
      PageTransition.tsx     (новый)
      ScrollToTop.tsx        (новый)
      ThemeToggle.tsx        (новый)
      Pagination.tsx         (новый)
      TableSkeleton.tsx      (новый)
      CardSkeleton.tsx       (новый)
      WelcomeDialog.tsx      (новый)
      BatchActions.tsx       (новый)
  providers/
    ThemeProvider.tsx        (новый)
  lib/
    toast.ts                 (новый)
    inn.ts                   (новый)
    export.ts                (новый)
    rating.ts                (новый)
    regions.ts               (новый)
  app/
    not-found.tsx            (новый)
    error.tsx                (новый)
```

## Что НЕ будем трогать (out of scope)

- FNS-интеграция (#28) — требует внешнего API, сложная, отдельной задачей
- Глобальный поиск — отдельная фича
- Drag-and-drop в дереве — если @dnd-kit слишком тяжёлый, отложим
- Онбординг
- Рассылка email